If bower not installed, run npm install -g bower
Run bower install
Run npm install
Run npm start
-- to see Redis logging, stop 'Redis server' windows service instead of stopping command running in other terminal like book says